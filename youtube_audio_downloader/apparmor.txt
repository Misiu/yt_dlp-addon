#include <tunables/global>

profile youtube_audio_downloader flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  signal (send) set=(kill,term,int,hup,cont),

  /init ix,
  /bin/** rix,
  /usr/bin/** rix,
  /usr/lib/** mr,
  /usr/local/lib/** mr,
  /lib/** mr,
  /opt/youtube-audio/** rixm,
  /app/** r,
  /etc/services.d/** rix,
  /etc/passwd r,
  /etc/group r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/ssl/** r,
  /etc/ca-certificates/** r,
  /package/** rix,
  /command/** rix,
  /run/{,**} rwk,
  /dev/null rw,
  /dev/urandom r,
  /proc/** r,
  /sys/devices/system/cpu/** r,
  /tmp/** rwk,
  /data/** rwk,
  /media/** rwk,
}
